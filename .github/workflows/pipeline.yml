name: workflow
on: 
  repository_dispatch:
    types:
      - test
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo "field: ${{ github.event.client_payload.application }}"
          echo "field: ${{ github.event.client_payload.environment }}"
          echo "field: ${{ github.event.client_payload.version }}"