name: workflow
on: 
  repository_dispatch:
    types:
      - test
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: 'echo "field: ${{ github.event.client_payload.application }}"'
      - run: 'echo "field: ${{ github.event.client_payload.environment }}"'
      - run: 'echo "field: ${{ github.event.client_payload.version }}"'